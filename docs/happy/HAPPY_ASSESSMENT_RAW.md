# Happy Phase 0 — Automated Assessment (raw)

_Generated by `scripts/happy_assess.py`. Read-only. Use this as the input to `docs/happy/HAPPY_ASSESSMENT.md`, not as a replacement for it._

## 0. Run metadata

- Generated: `2026-08-23T19:13:04.381078+00:00`
- Repo: `/Users/sebastiansabo/Documents/Git/JARVIS`
- Commit: `d24b4cf85`
- Branch: `staging`
- Working tree: DIRTY — commit or stash before Phase 1
- Python: `3.13.2`

## 1. Expected integration points

| Claim | Path | Status | Lines |
|---|---|---|---|
| push pipeline | `jarvis/core/notifications/push_service.py` | FOUND | 348 |
| in-app notify helper | `jarvis/core/notifications/notify.py` | FOUND | 119 |
| in-app notif repo | `jarvis/core/notifications/repositories/in_app_repo.py` | FOUND | 83 |
| notification routes | `jarvis/core/notifications/routes.py` | FOUND | 398 |
| base repository | `jarvis/core/base_repository.py` | FOUND | 122 |
| migration entry point | `jarvis/migrations/init_schema.py` | FOUND | 87 |
| roles/permissions schema | `jarvis/migrations/domains/schema_roles.py` | FOUND | 1252 |
| hr schema (schema idiom) | `jarvis/migrations/domains/schema_hr.py` | FOUND | 228 |
| permission decorators | `jarvis/core/roles/decorators.py` | FOUND | 39 |
| mobile shared/jwt | `jarvis/core/mobile/routes/_shared.py` | FOUND | 264 |
| mobile dashboard | `jarvis/core/mobile/routes/dashboard.py` | FOUND | 86 |
| media proxy | `jarvis/core/media/routes.py` | FOUND | 70 |
| deeplink | `jarvis/core/deeplink/routes.py` | FOUND | 148 |
| digest/chat blueprint | `jarvis/chat/routes.py` | FOUND | 357 |
| digest schema | `jarvis/migrations/domains/schema_digest.py` | FOUND | 169 |
| app factory | `jarvis/app.py` | FOUND | 677 |
| scheduler | `jarvis/tasks/cleanup.py` | FOUND | 637 |
| frontend api client | `jarvis/frontend/src/api/client.ts` | FOUND | 111 |
| dashboard widgets | `jarvis/frontend/src/pages/Dashboard/widgets.tsx` | FOUND | 381 |
| dashboard widget types | `jarvis/frontend/src/pages/Dashboard/types.ts` | FOUND | 102 |
| dashboard prefs | `jarvis/frontend/src/pages/Dashboard/useDashboardPrefs.ts` | FOUND | 148 |
| hub page | `jarvis/frontend/src/pages/Hub/index.tsx` | FOUND | 2501 |
| app router | `jarvis/frontend/src/App.tsx` | FOUND | 272 |
| frontend package.json | `jarvis/frontend/package.json` | FOUND | 62 |
| pytest conftest | `jarvis/conftest.py` | FOUND | 27 |
| playwright config | `playwright.config.js` | FOUND | 81 |

## 2. Verified function signatures

These are the exact contracts later agents must call. Copy them into HAPPY_ASSESSMENT.md §2.

**`jarvis/core/notifications/push_service.py`**

```python
L217: def send_push_to_users(user_ids, title, body, data=None, category='system', bypass_rules=False)
```
> `send_push_to_user` NOT FOUND — spec assumption invalid.

**`jarvis/core/notifications/notify.py`**

```python
L25: def notify_user(user_id, title, message=None, link=None, entity_type=None, entity_id=None, type='info', category='system')
L49: def notify_users(user_ids, title, message=None, link=None, entity_type=None, entity_id=None, type='info', category='system')
L75: def notify_with_push(user_ids, title, message=None, link=None, entity_type=None, entity_id=None, type='info', push_data=None, category='system')
```

**`jarvis/core/base_repository.py`**

```python
L45: def query_one(self, sql, params=None)
L58: def query_all(self, sql, params=None)
L71: def execute(self, sql, params=None, returning=False)
L100: def execute_many(self, callback)
```
> `transaction` NOT FOUND — spec assumption invalid.

**`jarvis/core/mobile/routes/_shared.py`**

```python
L168: def jwt_required(f)
L190: def _current_mobile_user()
L194: def _user_json(user)
```

**`jarvis/core/roles/decorators.py`**

```python
L8: def v2_permission_required(module, entity, action)
L21: def decorator(f)
L23: def decorated(*args, **kwargs)
```

## 3. Declared database tables (from migrations)

Total declared: **232**

- **ai_agent** (2): `ai_agent.learned_knowledge`, `ai_agent.message_feedback`
- **hr** (13): `hr.bonus_types`, `hr.course_activity`, `hr.course_certifications`, `hr.course_enrollments`, `hr.course_invoices`, `hr.course_transactions`, `hr.course_types`, `hr.courses`, `hr.enrollment_costs`, `hr.event_bonus_days`, `hr.event_bonuses`, `hr.events`, `hr.time_bank_transactions`
- **public** (217): `allocations`, `approval_audit_log`, `approval_decisions`, `approval_delegations`, `approval_flows`, `approval_requests`, `approval_steps`, `auto_tag_rules`, `bab_entries`, `bab_eur_rates`, `bab_report_config`, `bab_subtotal_refs`, `bab_uploads`, `bank_statement_transactions`, `bank_statements`, `bilant_generations`, `bilant_metric_configs`, `bilant_metrics`, `bilant_results`, `bilant_template_rows`, `bilant_templates`, `biostar_employees`, `biostar_punch_logs`, `carpark_appointments`, `carpark_document_templates`, `carpark_equipment_categories`, `carpark_equipment_items`, `carpark_interested_contacts`, `carpark_locations`, `carpark_mileage_history`, `carpark_modification_history`, `carpark_offers`, `carpark_pending_price_changes`, `carpark_pricing_history`, `carpark_pricing_rule_vehicles`, `carpark_pricing_rules`, `carpark_promotion_vehicles`, `carpark_promotions`, `carpark_publishing_platforms`, `carpark_publishing_sync_log`, `carpark_reservations`, `carpark_status_history`, `carpark_transfers`, `carpark_trip_log`, `carpark_vehicle_cost_lines`, `carpark_vehicle_costs`, `carpark_vehicle_documents`, `carpark_vehicle_equipment`, `carpark_vehicle_invoices`, `carpark_vehicle_links`, `carpark_vehicle_listings`, `carpark_vehicle_photos`, `carpark_vehicle_revenues`, `carpark_vehicles`, `carpark_vin_cache`, `chart_of_accounts`, `checkin_locations`, `checkin_nfc_tags`, `client_fleet`, `client_phones`, `client_profiles`, `companies`, `company_aliases`, `connecteam_form_submissions`, `connecteam_users`, `connector_sync_log`, `connectors`, `crm_client_audit`, `crm_client_contacts`, `crm_clients`, `crm_deals`, `crm_import_batches`, `crm_leads`, `department_structure`, `digest_channel_members`, `digest_channel_targets`, `digest_channels`, `digest_poll_options`, `digest_poll_votes`, `digest_polls`, `digest_posts`, `digest_reactions`, `digest_read_status`, `dms_audit_log`, `dms_categories`, `dms_documents`, `dms_drive_sync`, `dms_files`, `dms_folder_acl`, `dms_folders`, `dms_module_links`, `dms_party_roles`, `dms_relationship_types`, `document_parties`, `document_signatures`, `document_wml`, `document_wml_chunks`, `dropdown_options`, `efactura_company_connections`, `efactura_invoice_artifacts`, `efactura_invoice_refs`, `efactura_invoices`, `efactura_oauth_tokens`, `efactura_supplier_mapping_types`, `efactura_supplier_mappings`, `efactura_supplier_types`, `efactura_sync_errors`, `efactura_sync_runs`, `entity_tags`, `eval_assignments`, `eval_audit_log`, `eval_competencies`, `eval_cycles`, `eval_development_plans`, `eval_devplan_checkins`, `eval_events`, `eval_form_templates`, `eval_nudges`, `eval_participants`, `eval_question_blocks`, `eval_reports`, `eval_responses`, `facturare_document_numbers`, `facturare_generations`, `foi_de_parcurs`, `foi_de_parcurs_audit`, `foi_parcurs_session_events`, `form_submissions`, `forms`, `fp_archive_reasons`, `fp_clients`, `fp_company_config`, `fp_dealer_config`, `fp_km_configs`, `fp_lockout_reasons`, `fp_route_sheets`, `fp_routes`, `fp_vehicle_blocks`, `fp_vehicle_inspections`, `fp_vehicles`, `hr_dept_pulse_votes`, `invoice_observers`, `invoice_templates`, `invoices`, `kam_task_follow_ups`, `kam_visit_notes`, `kam_visit_plans`, `kam_visit_routes`, `kam_visit_tasks`, `leave_permit_conversion_submissions`, `leave_permit_conversions`, `mkt_budget_lines`, `mkt_budget_transactions`, `mkt_key_results`, `mkt_kpi_budget_lines`, `mkt_kpi_deal_sources`, `mkt_kpi_deals`, `mkt_kpi_definitions`, `mkt_kpi_dependencies`, `mkt_kpi_snapshots`, `mkt_objectives`, `mkt_project_activity`, `mkt_project_clients`, `mkt_project_comments`, `mkt_project_dms_links`, `mkt_project_events`, `mkt_project_files`, `mkt_project_kpis`, `mkt_project_members`, `mkt_projects`, `mkt_sim_benchmarks`, `mobile_devices`, `module_menu_items`, `notification_log`, `notification_settings`, `notifications`, `otp_codes`, `password_reset_tokens`, `permissions`, `permissions_v2`, `public_holidays`, `reinvoice_destinations`, `role_permissions`, `role_permissions_v2`, `roles`, `schema_version`, `service_catalog`, `service_catalog_pricing`, `sincron_activity_codes`, `sincron_co_balance`, `sincron_co_import_runs`, `sincron_employees`, `sincron_org_members`, `sincron_org_nodes`, `sincron_schedule_history`, `sincron_sync_runs`, `sincron_timesheets`, `smart_notification_state`, `structure_node_members`, `structure_nodes`, `suppliers`, `tag_groups`, `tags`, `telemetry_events`, `telemetry_sessions`, `theme_settings`, `ticket_comments`, `tickets`, `user_events`, `user_filter_presets`, `users`, `vat_rates`, `vendor_mappings`, `verification_discrepancies`, `verification_runs`, `voucher_service_catalog`, `vouchers`

### 3.1 Name collisions with Happy

| Token | Colliding tables |
|---|---|
| `campaign` | none |
| `announcement` | none |
| `banner` | none |
| `kudos` | none |
| `pulse` | `hr_dept_pulse_votes` |
| `happy` | none |
| `spotlight` | none |
| `marquee` | none |
| `digest` | `digest_channels`, `digest_channel_members`, `digest_posts`, `digest_reactions`, `digest_polls`, `digest_poll_options`, `digest_poll_votes`, `digest_channel_targets`, `digest_read_status` |

### 3.2 Non-public schema idiom (copy this for `happy`)

```sql
CREATE SCHEMA IF NOT EXISTS hr')
```

### 3.3 Migration registration point

Lines in `init_schema.py` that invoke a domain schema builder (register `create_schema_happy` alongside these):

```python
    create_schema_core(conn, cursor)
    create_schema_statements(conn, cursor)
    create_schema_roles(conn, cursor)
    create_schema_misc(conn, cursor)
    create_schema_hr(conn, cursor)
    create_schema_efactura(conn, cursor)
    create_schema_approvals(conn, cursor)
    create_schema_marketing(conn, cursor)
    create_schema_signatures(conn, cursor)
    create_schema_bilant(conn, cursor)
    create_schema_crm(conn, cursor)
    create_schema_field_sales(conn, cursor)
    create_schema_forms(conn, cursor)
    create_schema_digest(conn, cursor)
    create_schema_carpark(conn, cursor)
    create_schema_sincron(conn, cursor)
    create_schema_connecteam(conn, cursor)
    create_schema_holidays(conn, cursor)
    create_schema_telemetry(conn, cursor)
    create_schema_courses(conn, cursor)
    create_schema_facturare(conn, cursor)
    create_schema_time_bank(conn, cursor)
    create_schema_ticketing(conn, cursor)
    create_schema_controlling_bab(conn, cursor)
    create_schema_vouchers(conn, cursor)
    create_schema_evaluation360(conn, cursor)
    create_schema_incremental(conn, cursor)
```

## 4. Route surface

Total Flask routes declared: **1428**

| Blueprint | Routes | Example |
|---|---|---|
| `carpark_bp` | 120 | `/analytics/activity` |
| `marketing_bp` | 107 | `/api/budget-lines/<int:line_id>/transactions` |
| `dms_bp` | 83 | `/api/dms/audit` |
| `efactura_bp` | 81 | `/` |
| `foi_parcurs_bp` | 77 | `/api/foi-parcurs/archive-reasons` |
| `events_bp` | 75 | `/` |
| `biostar_bp` | 54 | `/api/adjustments` |
| `eval360_bp` | 53 | `/api/assignments/<int:assignment_id>/comment-nudge` |
| `facturare_bp` | 45 | `/facturare/api/anexa-lines` |
| `org_bp` | 41 | `/api/brands-all` |
| `crm_bp` | 40 | `/api/crm/clients` |
| `invoices_bp` | 39 | `/accounting` |
| `bilant_bp` | 37 | `/api/ai-models` |
| `sincron_bp` | 36 | `/api/activity-codes` |
| `courses_bp` | 32 | `/api/certifications` |
| `mobile_bp` | 30 | `/api/auth/current-user` |
| `chat_bp` | 30 | `/channels` |
| `field_sales_bp` | 30 | `/api/field-sales/clients/<int:client_id>` |
| `statements_bp` | 29 | `/` |
| `vouchers_bp` | 28 | `/api/public/vouchers/lookup/<code>` |
| `settings_bp` | 27 | `/api/dropdown-options` |
| `auth_bp` | 27 | `/api/auth/change-password` |
| `ai_agent_bp` | 27 | `/` |
| `profile_bp` | 26 | `/` |
| `approvals_bp` | 25 | `/api/audit` |
| `connecteam_bp` | 23 | `/api/approvers` |
| `tags_bp` | 21 | `/api/auto-tag-rules` |
| `notifications_bp` | 20 | `/api/commit-digest/status` |
| `controlling_bab_bp` | 19 | `/controlling/bab/api/analyze` |
| `forms_bp` | 19 | `/api/forms` |
| `flask_app` | 14 | `/` |
| `roles_bp` | 13 | `/api/permissions` |
| `time_bank_bp` | 12 | `/api/time-bank/approve/<int:tx_id>` |
| `telemetry_bp` | 9 | `/api/admin/telemetry/dashboard` |
| `ticketing_bp` | 9 | `/api/tickets` |
| `autovit_bp` | 8 | `/api/accounts/<int:account_id>/adverts` |
| `templates_bp` | 7 | `/api/templates` |
| `identity_bp` | 7 | `/api/auto-map` |
| `co_balance_bp` | 7 | `/api/co-balance` |
| `bugetare_bp` | 6 | `/api/bulk/export` |
| `signatures_bp` | 6 | `/api/<int:sig_id>` |
| `checkin_bp` | 6 | `/api/locations` |
| `push_bp` | 5 | `/api/config` |
| `presets_bp` | 4 | `/api/presets` |
| `drive_bp` | 4 | `/api/drive/folder-link` |
| `connectors_bp` | 3 | `/api/connectors` |
| `verification_bp` | 3 | `/api/resolve/<int:discrepancy_id>` |
| `deeplink_bp` | 2 | `/go/approval/<int:request_id>` |
| `media_bp` | 1 | `/api/media/<path:key>` |
| `hr_bp` | 1 | `/` |

### 4.1 Prefix collisions

- `/api/happy` → free
- `/api/mobile/happy` → free
- `/go/happy` → free

### 4.2 Mobile route inventory

```
/api/mobile/current-user
/api/mobile/dashboard
/api/mobile/notify-update
/api/mobile/version
/api/mobile/widget-data
```

## 5. Frontend


### 5.1 Dependencies present (no new packages without approval)

- `react`: PRESENT
- `@tanstack/react-query`: PRESENT
- `radix-ui`: PRESENT
- `lucide-react`: PRESENT
- `react-markdown`: PRESENT
- `remark-gfm`: PRESENT
- `sonner`: PRESENT
- `zustand`: PRESENT
- `react-hook-form`: PRESENT
- `zod`: PRESENT
- `tailwindcss`: PRESENT
- `react-grid-layout`: PRESENT
- `recharts`: PRESENT
- `next-themes`: PRESENT

### 5.2 shadcn/ui primitives

| Component | Status |
|---|---|
| `dialog` | present |
| `drawer` | **must add: npx shadcn add drawer** |
| `sheet` | present |
| `badge` | present |
| `checkbox` | present |
| `card` | present |
| `button` | present |
| `skeleton` | present |
| `progress` | present |
| `tabs` | present |
| `select` | present |
| `textarea` | present |
| `avatar` | present |
| `separator` | present |

### 5.3 Dashboard widget registry


`jarvis/frontend/src/pages/Dashboard/types.ts`:
```ts
import {
  FileText, Users, ClipboardCheck, CreditCard,
  CalendarDays, Megaphone, Bell,
} from 'lucide-react'
import type { User } from '@/types'

export interface WidgetLayout {
  i: string
  x: number
  y: number
  w: number
  h: number
  minW?: number
  maxW?: number
  minH?: number
  maxH?: number
}

export interface WidgetDef {
  id: string
  name: string
  icon: typeof FileText
  permission?: keyof User
  defaultLayout: { w: number; h: number; minW?: number; minH?: number }
  defaultVisible: boolean
  statCards: { key: string; title: string; icon: typeof FileText }[]
}

export interface WidgetPref {
  id: string
  visible: boolean
  layout: WidgetLayout
}

export interface DashboardPreferences {
  version: number
  widgets: WidgetPref[]
}

export const WIDGET_CATALOG: WidgetDef[] = [
  {
    id: 'accounting_invoices',
    name: 'Recent Invoices',
    icon: FileText,
    permission: 'can_access_accounting',
    defaultLayout: { w: 6, h: 4, minW: 3, minH: 3 },
    defaultVisible: true,
    statCards: [
      { key: 'total_invoices', title: 'Total Invoices', icon: FileText },
    ],
  },
  {
    id: 'statements_summary',
    name: 'Bank Statements',
    icon: CreditCard,
    permission: 'can_access_statements',
    defaultLayout: { w: 2, h: 3, minW: 2, minH: 2 },
    defaultVisible: true,
    statCards: [{ key: 'pending_txns', title: 'Pending Txns', icon: CreditCard }],
  },
  {
    id: 'hr_summary',
    name: 'HR Overview',
    icon: CalendarDays,
    permission: 'can_access_hr',
    defaultLayout: { w: 2, h: 3, minW: 2, minH: 2 },
    defaultVisible: true,
    statCar
```

`jarvis/frontend/src/pages/Dashboard/useDashboardPrefs.ts`:
```ts
import { useState, useEffect, useMemo, useCallback } from 'react'
import type { User } from '@/types'
import { WIDGET_CATALOG, type DashboardPreferences, type WidgetPref, type WidgetLayout } from './types'

const STORAGE_KEY = 'jarvis_dashboard_prefs'
const CURRENT_VERSION = 2
const COLS = 6

function hasPermission(user: User | null, permission?: keyof User): boolean {
  if (!permission) return true
  return !!user?.[permission]
}

/** Auto-place widgets in a 6-col grid using row-packing. */
function autoPlace(widgets: { id: string; visible: boolean }[]): WidgetPref[] {
  const catalogMap = new Map(WIDGET_CATALOG.map(w => [w.id, w]))
  let x = 0, y = 0, rowMaxH = 0
  return widgets.map(w => {
    const def = catalogMap.get(w.id)
    const width = def?.defaultLayout.w ?? 2
    const height = def?.defaultLayout.h ?? 3
    if (x + width > COLS) { x = 0; y += rowMaxH; rowMaxH = 0 }
    const layout: WidgetLayout = {
      i: w.id, x, y, w: width, h: height,
      minW: def?.defaultLayout.minW, minH: def?.defaultLayout.minH,
    }
    x += width
    rowMaxH = Math.max(rowMaxH, height)
    return { id: w.id, visible: w.visible, layout }
  })
}

function buildDefaults(): DashboardPreferences {
  const widgets = WIDGET_CATALOG.map(w => ({ id: w.id, visible: w.defaultVisible }))
  return { version: CURRENT_VERSION, widgets: autoPlace(widgets) }
}

function loadPrefs(): DashboardPreferences {
  try {
    const raw = localStorage.getItem(STORAGE_KEY)
    if (!raw) return buildDefaults()
    const parsed = JSON.parse(raw)

    // Migrate v1 → v2: convert order-based to layout-based
   
```

### 5.4 Hub insertion point

- L195: `// Notifications`
- L198: `queryFn: () => notificationsApi.getNotifications({ limit: 15 }),`
- L401: `<HubAppsCard tiles={visibleTiles} onSelect={setActiveModule} />`
- L404: `<HubBonusCard />`
- L407: `{/* Right 1/3 — Notifications + Punch Card */}`
- L414: `Notifications`
- L466: `<HubWorkSummaryCard />`
- L522: `function HubAppsCard({ tiles, onSelect }: { tiles: AppTile[]; onSelect: (key: NonNullable<ActiveModule>) => vo`
- L2322: `function HubWorkSummaryCard() {`
- L2408: `function HubBonusCard() {`

> Insert `<MarqueeWidget placement="hub_card" />` immediately above the Notifications `<Card>` in the right column. Touch nothing else in this file.

### 5.5 i18n strategy

i18n library references: NONE FOUND
> If none: campaign content is single-locale. Store `locale` on the campaign anyway; do not build a translation layer in Phase 1.

## 6. Background jobs

Registration idiom for the Happy nightly jobs (`refresh_targets`, `purge_events`, `rollup_stats`, `expire_points`):

```python
Task scheduler — registers all background jobs.
Only one worker starts the scheduler (file-lock guard) to avoid
from apscheduler.schedulers.background import BackgroundScheduler
from tasks.sincron import sync_sincron_timesheets
scheduler = BackgroundScheduler(daemon=True)
_scheduler_deferred = False  # True when another worker holds the lock
def _acquire_scheduler_lock():
        lock_path = os.path.join(os.path.dirname(__file__), '..', '.scheduler.lock')
def start_scheduler():
    """Start the background scheduler with all cleanup jobs.
    Uses a file lock so only one gunicorn worker runs the scheduler.
    if scheduler.running:
    global _scheduler_deferred
    if not _acquire_scheduler_lock():
        _scheduler_deferred = True
    scheduler.add_job(
        'interval',
    scheduler.add_job(
        'interval',
    scheduler.add_job(
        'interval',
    scheduler.add_job(
        'interval',
    scheduler.add_job(
        'interval',
    scheduler.add_job(
        'interval',
    scheduler.add_job(
        'cron',
    scheduler.add_job(
        'cron',
    scheduler.add_job(
        'interval',
    scheduler.add_job(
        'cron',
    scheduler.add_job(
        'cron',
    scheduler.add_job(
        'interval',
    scheduler.add_job(
```

## 7. Test conventions

- pytest files: **125**, subdirectories: __pycache__, accounting, approvals, auth, biostar, carpark, connecteam, core, crm, dept_pulse, efactura, evaluation360, foi_parcurs, forms, hr, hr_events, migrations, org, tasks
- conftest fixtures: <none detected>
- vitest files: **52**
- playwright config: present

## 8. Production data reality


### 8.1 Expected tables present

| Table | Present | Rows |
|---|---|---|
| `public.users` | yes | 289 |
| `public.roles` | yes | 7 |
| `public.notifications` | yes | 11251 |
| `public.mobile_devices` | yes | 10 |
| `public.permissions_v2` | yes | 254 |
| `public.role_permissions_v2` | yes | 1464 |
| `public.department_structure` | yes | 33 |
| `public.structure_nodes` | yes | 105 |
| `hr.events` | yes | 31 |
| `public.digest_channels` | yes | 3 |
| `public.digest_posts` | yes | 25 |
| `public.digest_read_status` | yes | 42 |
| `public.hr_dept_pulse_votes` | yes | 16 |

### 8.2 Targeting-column coverage on users (active only)

| Column | Non-null | % of active | Distinct |
|---|---|---|---|
| `company` | 261 | 97% | 15 |
| `brand` | 36 | 13% ⚠️ | 11 |
| `department` | 176 | 65% ⚠️ | 45 |
| `subdepartment` | 1 | 0% ⚠️ | 1 |
| `org_unit_id` | 0 | 0% ⚠️ | 0 |
| `contract_status` | 269 | 100% | 1 |

Active users: **269**
> Any column below 80% coverage cannot be used as a primary audience dimension without a data-cleanup task in Phase 1.

### 8.3 Department sizes — drives the pulse anonymity rollup

- Departments: **46**
- **Below the min_group_size of 5: 37** → these cohorts must roll up to their parent unit

| Department | Active |
|---|---|
| VW / PLR | 1 |
| AAP | 1 |
| Specialist garantii | 1 |
| Coordonator Vanzari | 1 |
| CEO | 1 |
| DWA Dispo | 1 |
| Vanzari LKW | 1 |
| ITP | 1 |
| Administrativ | 1 |
| Volvo/ Asigurari | 1 |
| Consilier Service | 1 |
| Audi / PLR | 1 |
| Consilier caroserie | 1 |
| COO | 1 |
| Volvo/ Magazie | 1 |
| CAO | 1 |
| Export | 1 |
| Director Service | 1 |
| VW / Asist Manager | 1 |
| Garantii | 1 |
| Key Account Manager | 1 |
| Programari | 1 |
| Coordonator Rec. | 1 |
| Volvo/ Daune | 1 |
| Audi/ Asigurari | 1 |

### 8.4 Push reach ceiling

- Registered devices: **10** across **8** users
- Push-reachable share of active users: **3%** — this is the hard ceiling on any push-based KPI

### 8.5 hr.events volume (auto-generated event campaigns)

- rows: 31, range 2026-01-22 → 2026-08-13

## 9. Verdict — fill this in by hand

| Integration point | GO / GO-WITH-CHANGES / NO-GO | Evidence | Note |
|---|---|---|---|
| Push pipeline reuse | | | |
| Permissions v2 seeding | | | |
| Mobile payload extension | | | |
| Media upload + proxy | | | |
| Dashboard widget slot | | | |
| Hub slot | | | |
| Nightly job registration | | | |
| Audience targeting data quality | | | |
| Pulse cohort viability | | | |
| i18n strategy | | | |

**Phase 1 may / may not begin.**

